{
  "name": "ptit-bach-hoa-xanh",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix fontend && npm run build --prefix fontend",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix fontend\""
  }
}
